import styled from 'styled-components';
import Common from '../../styles/common';

const HeaderWrapper = styled.div`
  width: 100%;
  height: auto;
  padding: 50px 0;
`;

const S = {
  HeaderWrapper,
};

export default S;
